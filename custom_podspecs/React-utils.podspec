Pod::Spec.new do |s|
  s.name         = "React-utils"
  s.version      = "0.1.0"
  s.summary      = "Minimal placeholder podspec for React-utils."
  s.description  = "A minimal placeholder podspec for React-utils."
  s.homepage     = "https://reactnative.dev"
  s.license      = "MIT"
  s.author       = { "Facebook" => "opensource@fb.com" }
  s.source       = { :git => "https://github.com/facebook/react-native.git", :tag => "v0.1.0" }
  s.platform     = :ios, "10.0"
  s.source_files = "*.h"
end